# Claude Project Memory

Read `AGENTS.md` first.

This repository references the shared cross-repository rules in [`tensor4all/tensor4all-agent-rules`](https://github.com/tensor4all/tensor4all-agent-rules) and uses `AGENTS.md` as the canonical wrapper.
